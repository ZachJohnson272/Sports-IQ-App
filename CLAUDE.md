# Sports-IQ-App — Claude Code Instructions

## Git Workflow

1. When asked to make any change: commit it locally, then push to a **feature branch** (never directly to `main`).
2. Use descriptive branch names like `feature/warriors-button` or `fix/dropdown-counts`.
3. **Wait** for the user to say "merge it" before creating a PR and merging to main.
